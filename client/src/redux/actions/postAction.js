import { getDataAPI, postDataAPI, putDataAPI } from '../../api/api';
import * as ACTIONS from "../constants/postContant";


export const addPost = (data) => async (dispatch) => {
    try {
        
    } catch (err) {
        console.log(err);
    }
}

export const getAllPosts = (data) => async (dispatch) => {
    try {
        
    } catch (err) {
        console.log(err);
    }
}

export const getHotPosts = (data) => async (dispatch) => {
    try {
        
    } catch (err) {
        console.log(err);
    }
}

export const getCodenamePosts = (data) => async (dispatch) => {
    try {
        
    } catch (err) {
        console.log(err);
    }
}

export const getSavedPosts = (data) => async (dispatch) => {
    try {
        
    } catch (err) {
        console.log(err);
    }
}
